# https://taskfile.dev

version: '3'

vars:
  VERSION: v2.0.0-rc.19

tasks:
  init:
    desc: Init
    cmds:
      - npx cdk@{{.VERSION}} init app --language go
      - go mod tidy
  pre_build:
    desc: pre_build
  build:
    desc: build    
  diff:
    desc: diff
    cmds:
      - npx cdk@{{.VERSION}} diff
  deploy:
    desc: deploy
    cmds:
      - npx cdk@{{.VERSION}} deploy  
  show:
    desc: show  
    cmds: 
      - cdkstat
  remove:
    desc: remove  
    cmds: 
      - npx cdk@{{.VERSION}} destroy -f
